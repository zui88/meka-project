Interface function

visual mode:

	./detector --visual-mode

raw string mode:

	./detector

due to start in visual mode the optional parameter '--visual-mode' has
to match exaclty others the detector will send the information via
raw-bit-mode
